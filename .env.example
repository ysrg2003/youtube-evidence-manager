# YouTube Data API v3 key. Keep this value secret and never commit the real key.
YOUTUBE_API_KEY=REPLACE_WITH_YOUTUBE_API_KEY

# Optional. Used only when the evidence CLI is called with --analyze.
GEMINI_API_KEY=REPLACE_WITH_GEMINI_API_KEY
GEMINI_MODEL=gemini-2.5-flash
